{% comment %}
  Seção Hero para página de Segurança
{% endcomment %}

<div class="seguranca-hero" style="background-color: {{ section.settings.background_color }}; color: {{ section.settings.text_color }};">
  <div class="seguranca-hero__inner">
    <div class="seguranca-hero__content">
      <h1 class="seguranca-hero__title">{{ section.settings.title }}</h1>
      <p class="seguranca-hero__subtitle">{{ section.settings.subtitle }}</p>
    </div>
  </div>
</div>

<style>
  .seguranca-hero {
    padding: 80px 0;
    text-align: center;
  }
  
  .seguranca-hero__inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }
  
  .seguranca-hero__title {
    font-size: 48px;
    font-weight: 700;
    line-height: 1.2;
    margin: 0 0 16px 0;
    font-family: Inter, sans-serif;
  }
  
  .seguranca-hero__subtitle {
    font-size: 20px;
    font-weight: 400;
    line-height: 1.5;
    margin: 0;
    opacity: 0.9;
  }
  
  @media (max-width: 768px) {
    .seguranca-hero {
      padding: 60px 0;
    }
    
    .seguranca-hero__title {
      font-size: 36px;
    }
    
    .seguranca-hero__subtitle {
      font-size: 18px;
    }
  }
</style>

{% schema %}
{
  "name": "Hero Segurança",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Título",
      "default": "Segurança"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtítulo",
      "default": "Sua segurança e privacidade são nossas prioridades"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Cor de fundo",
      "default": "#5A3838"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Cor do texto",
      "default": "#FFFFFF"
    }
  ],
  "presets": [
    {
      "name": "Hero Segurança"
    }
  ]
}
{% endschema %}

